package  {		import flash.display.MovieClip;		import flash.events.KeyboardEvent;		import flash.ui.Keyboard;		import flash.text.TextField;   	import flash.text.TextFormat;  	import flash.text.TextFormatAlign;			public class ChatBox extends MovieClip{		public function ChatBox() {						stage.addEventListener(KeyboardEvent.KEY_DOWN, onKeyPress, false, 0, true);			stage.addEventListener(KeyboardEvent.KEY_UP, onKeyRelease, false, 0, true);						//FORMAT TEXT			var inputformat = new TextFormat();			inputformat.letterSpacing = .75;			chatbox.inputtext.setTextFormat(inputformat); 						var outputformat = new TextFormat();			outputformat.letterSpacing = .75;			chatbox.outputtext.setTextFormat(outputformat); 								}				private function onKeyPress(event:KeyboardEvent):void{			var key:uint = event.keyCode;			switch (key) {				case Keyboard.ENTER :					//					break;			}		}				private function onKeyRelease(event:KeyboardEvent):void{			var key:uint = event.keyCode;			switch (key) {				case Keyboard.ENTER :					//					break;			}		}			}	}